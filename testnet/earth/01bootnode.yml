version: "3.4"

services:

  bootnode:
    image: tomochain/infra-bootnode:devnet
    volumes:
      - bootnode:/tomochain
    ports:
      - 30301:30301
      - 30301:30301/udp
    deploy:
      placement:
        constraints:
          - node.role == manager

volumes:
  bootnode:
