version: "3.4"

services:

  bootnode:
    image: tomochain/infra-bootnode:devnet
    volumes:
      - bootnodes:/tomochain/bootnodes
    deploy:
      placement:
        constraints:
          - node.role == manager

volumes:
  bootnodes:
