version: "3.4"

services:

  bootnode:
    image: tomochain/infra-bootnode:devnet
    ports:
      - 39391:39391
    volumes:
      - bootnode:/tomochain
    deploy:
      placement:
        constraints:
          - node.role == manager

volumes:
  bootnode:
